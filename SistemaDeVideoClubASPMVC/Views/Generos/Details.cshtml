@model  SistemaDeVideoClubASPMVC.Models.Genero

@{
    ViewBag.Title = "Details";
}
<h2>Detalles</h2>

<div>
    <h4>Genero</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Descripcion)
        </dd>

    </dl>
</div>
<p>
    <a href="@Url.Action("Edit", "Generos", new {id = Model.GeneroId})" class="btn btn-warning"><i class="fas fa-edit"></i> Editar</a>

    <a href="@Url.Action("Index", "Generos")" class="btn btn-info"><i class="fas fa-list"></i> Volver</a>
    @*@Html.ActionLink("Editar", "Edit", new { id = Model.GeneroId }) |
        @Html.ActionLink("Volver", "Index")*@
</p>