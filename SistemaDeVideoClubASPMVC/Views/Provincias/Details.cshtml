@model  SistemaDeVideoClubASPMVC.Models.Provincia

@{
    ViewBag.Title = "Details";
}

<h2>Detalles</h2>

<div>
    <h4>Provincia</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NombreProvincia)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NombreProvincia)
        </dd>

    </dl>
</div>
<p>

    <a href="@Url.Action("Edit", "Provincias", new {id = Model.ProvinciaId})" class="btn btn-warning"><i class="fas fa-edit"></i> Editar</a>

    <a href="@Url.Action("Index", "Provincias")" class="btn btn-info"><i class="fas fa-list"></i> Volver</a>

    @*@Html.ActionLink("Edit", "Edit", new { id = Model.ProvinciaId }) |
    @Html.ActionLink("Back to List", "Index")*@
</p>